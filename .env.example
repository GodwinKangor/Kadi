# URL of the realtime Kadi Socket.IO server.
# Leave unset for local dev / single-server deploys (Render), where the
# frontend is served by server.js itself and can use the same origin.
# Required when the frontend is deployed separately, e.g. on Vercel.
VITE_SERVER_URL=https://your-kadi-server.onrender.com
